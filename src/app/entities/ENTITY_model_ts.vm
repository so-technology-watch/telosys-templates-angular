// File generated by $generator.name ( version $generator.version ) - Date $today.date("yyyy-MM-dd") ( Time $today.time )

#set($LCentityName = $fn.uncapitalize($entity.name))
export interface $entity {
#foreach ( $attrib in $entity.attributes)
#set($LCattributeName = $fn.uncapitalize($attrib.name))
#if ( $attrib.type == "int" || $attrib.type == "Integer" || $attrib.type == "BigDecimal" || $attrib.type == "Short" )
	${LCentityName}_${LCattributeName}: number;
#elseif ($attrib.type == "String")
	${LCentityName}_${LCattributeName}: string;
#else
	${LCentityName}_${LCattributeName}: $attrib.type;
#end
#end
}
